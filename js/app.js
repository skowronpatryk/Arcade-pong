const gameMenu = document.querySelector('.gameInfo');

const pvp = document.querySelector('.pvp');
const pvpClick = document.querySelector('.firstOption');

const pvc = document.querySelector('.pvc');
const pvcClick = document.querySelector('.secondOption');

setTimeout(() => {
    window.location='./game.html';
}, 4000)
